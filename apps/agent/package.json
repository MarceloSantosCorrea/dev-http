{
  "name": "@devhttp/agent",
  "version": "0.1.0",
  "private": true,
  "productName": "DevHttp Agent",
  "description": "Agent local do DevHttp para execução de requests a localhost e rede privada.",
  "author": "Marcelo Correa",
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "dev:server": "node server.js",
    "build": "node --check main.js && node --check server.js",
    "start": "electron .",
    "start:server": "node server.js",
    "lint": "node --check main.js && node --check server.js",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win nsis",
    "dist:mac": "electron-builder --mac dmg",
    "dist:linux": "electron-builder --linux AppImage"
  },
  "dependencies": {
    "@devhttp/local-executor": "*"
  },
  "devDependencies": {
    "electron": "37.10.3",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.marcelosantoscorrea.devhttp.agent",
    "productName": "DevHttp Agent",
    "asar": true,
    "files": [
      "main.js",
      "server.js",
      "package.json"
    ],
    "directories": {
      "output": "dist"
    },
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.developer-tools"
    },
    "win": {
      "target": [
        "nsis"
      ]
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "category": "Development"
    }
  }
}
